[user]
  name = luathn
  email = luat1203@gmail.com

[includeIf "gitdir:~/workspace/"]
  path = .gitconfig-company

[includeIf "gitdir:~/work/"]
  path = .gitconfig-company

[core]
  editor = vim

[cola]
  spellcheck = false

; [core]
  ; pager = delta

; [delta]
;   line-numbers = true
;   side-by-side = true
;   plus-color = "#012800"
;   minus-color = "#340001"

; [interactive]
;   diffFilter = delta --color-only
