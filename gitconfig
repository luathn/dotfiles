[user]
  name = luathn
  email = luat1203@gmail.com

[includeIf "gitdir:~/workspace/"]
  path = .gitconfig-company

[includeIf "gitdir:~/work/"]
  path = .gitconfig-company

[core]
  editor = nvim
  pager = delta
  excludesfile = ~/.gitignore_global

[cola]
  spellcheck = false

[delta]
  line-numbers = true
  side-by-side = false
  syntax-theme = base16-256
  plus-style = 'white "#455052"'
  minus-style = 'white "#514251"'

[merge]
  conflictstyle = diff3

; [interactive]
;   diffFilter = delta --color-only
[push]
	default = current
[gpg]
	program = gpg
[commit]
	gpgsign = true
